import React from 'react';
import { type ITableItem } from '../utils/income';

interface IProps {
    items: ITableItem[];
}

export function TaxTable(_props: IProps): React.JSX.Element {
    return <></>;
}
